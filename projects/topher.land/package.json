{
  "private": true,
  "name": "topher.land",
  "type": "module",
  "scripts": {
    "restore": "terraform -chdir=terraform init",
    "plan": "terraform -chdir=terraform plan",
    "deploy": "terraform -chdir=terraform apply -auto-approve"
  }
}
