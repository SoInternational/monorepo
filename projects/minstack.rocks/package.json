{
  "private": true,
  "name": "minstack.rocks",
  "type": "module",
  "scripts": {
    "init": "terraform -chdir=terraform init",
    "test": "terraform -chdir=terraform validate",
    "plan": "terraform -chdir=terraform plan -lock=false -refresh=false",
    "deploy": "terraform -chdir=terraform apply -auto-approve"
  }
}
